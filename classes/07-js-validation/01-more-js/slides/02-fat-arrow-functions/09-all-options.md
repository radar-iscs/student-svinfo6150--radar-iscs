# All the options

```js
const greet = (target) => { 
  return `Hello, ${target}`;
};

const sayHello = target => {   
  return `Hello, ${target}`;
};

const sayHi = (target) => `Hi ${target}`;

const sayHey = target => `Hey ${target}`;

```
